// Tipos de tipos de datos por defectos
let vehiculos: string;
let cantidades: number;
let esAutomoviles: boolean;

// Valores por inferencia
let vehiculo = "automovil";
let cantidad = 1;
let esAutomovil = true;

cantidad = (2 * 4) / 10;
